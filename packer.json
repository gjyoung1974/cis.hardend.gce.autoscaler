{
  "variables": {
        "gcloud_account_json": "{{env `HOME`}}/.gcloud/account.json"
  },
  "builders": [
    {
      "type": "googlecompute",
      "account_file": "{{user `gcloud_account_json`}}",
      "project_id": "acme",
      "source_image": "centos-7-v20180716",
      "zone": "us-west2-a",
      "image_name": "acme-centos7-base-{{timestamp}}"
    }
  ],
  "provisioners": [
      {
           "type": "shell",
           "inline": [
                  "echo -n 'Hello World'",
                  "echo 'test' > /test.txt"
           ]
      }
  ]
}
